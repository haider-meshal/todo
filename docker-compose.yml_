version: '3'

services:
  db:
    build: ./db
    ports:
      - 1433:1433
  app:
    build:
      context: ./ToDo.API
      dockerfile: Dockerfile
    ports:
      - 8080:80
    depends_on:
      - db
